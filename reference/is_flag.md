# Check if a value is a logical flag

This function checks if a value is a logical scalar (i.e., a single
logical value).

## Usage

``` r
is_flag(x)
```

## Arguments

- x:

  A value to check.

## Value

A logical scalar indicating whether `x` is a logical flag.
