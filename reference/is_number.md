# Check if an object is a single number

Check if an object is a single number

## Usage

``` r
is_number(x)
```

## Arguments

- x:

  An object to check.

## Value

A logical value indicating whether `x` is a single number.
