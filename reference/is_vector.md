# Check if an object is a vector This function checks if an object is a vector

Check if an object is a vector This function checks if an object is a
vector

## Usage

``` r
is_vector(x)
```

## Arguments

- x:

  An object to check

## Value

A logical indicating whether `x` is a vector
