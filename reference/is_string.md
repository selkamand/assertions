# Check if an object is a single string

Check if an object is a single string

## Usage

``` r
is_string(x)
```

## Arguments

- x:

  An object to check.

## Value

A logical value indicating whether `x` is a single string.
