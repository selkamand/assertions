# Check if an object is a logical vector

Check if an object is a logical vector

## Usage

``` r
is_logical_vector(x)
```

## Arguments

- x:

  An object to check.

## Value

A logical value indicating whether `x` is a logical vector.
